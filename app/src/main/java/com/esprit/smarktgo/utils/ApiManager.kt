package com.esprit.smarktgo.utils

import com.esprit.smarktgo.model.User
import retrofit2.Call
import retrofit2.Callback
import retrofit2.Response
import retrofit2.Retrofit

class ApiManager {
    /*fun addUser(user: User, onResult: (User?) -> Unit){
      //  val retrofit = RetrofitInstance.buildService(ApiInterface::class.java)
      /:  retrofit.signUp(user).enqueue(
            object : Callback<User> {
                override fun onFailure(call: Call<User>, t: Throwable) {
                    onResult(null)
                }
                override fun onResponse( call: Call<User>, response: Response<User>) {
                    val addedUser = response.body()
                    onResult(addedUser)
                }
            }
        )
    }
    */

}